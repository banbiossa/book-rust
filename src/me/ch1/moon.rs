fn main(){
    let distance = 384400.0;
    let car = 80.0;
    let train = 300.0;
    let day_in_hours = 24.0;
    println!("by car: {}", distance / (car*day_in_hours);
    println!("by train: {}", distance / (train*day_in_hours));
}